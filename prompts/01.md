# Role (User) Prompt

Você é um desenvolvedor full stack sênior especializado em NextJS.

## Contexto

Você está trabalhando em um projeto de SaaS de agendamento de para barbearias, onde o usuário pode selecionar uma barbearia, serviço, data e realizar um agendamento.

Tecnologias utilizadas:

- React
- Next JS
- Prisma (veja schema em @prisma/schema.prisma)
- shadcn/ui
- Tailwind
- lucide-react
- npm
- node

### Regras

- SEMPRE use shadcn como biblioteca de componentes;
- NUNCA crie componentes do zero antes de verificar se há algum do shadcn que atinja seu objetivo;
- NUNCA use cores hard-coded do tailwind, apenas cores do tema que estão em: app\globals.css;
- SEMPRE use componentes que estão em @components\ui\page.tsx;
- SEMPRE use o MCP do Context7 para buscar documentações, sites e APIs;
- SEMPRE use o componente Image do NextJs para renderizar imagens;
- NUNCA chame o prisma de componentes. SEMPRE crie uma função em @data, assim como é feio em @app/page.tsx;
- SEMPRE use rem para medidas, nunca px.

### Tarefa

Visão Geral da Tarefa

Crie a página que está em [](https://www.figma.com/design/KBlNBjp5XXWUj64ZCiT9lq/Aparatus?node-id=10-6869&m=dev usando) Figma MCP em @app/barbershops/[id]/page.tsx.

Seja 100% fiel ao Figma **CUSTE O QUE CUSTAR**.

Requisitos Funcionais

- O botão de "Reservar" não deve fazer nada.
- O botão de "Copiar" telefone deve copiar o telefone para o clipboard.
- Os dados da barbearia devem ser recuperados do banco de dados, usando o ID recebido na URL.
- A imagem do banner da página no topo deve ser a imagem da barbearia no banco de dados (imageUrl).
- O botão de "Voltar" no banner da página deve voltar à tela anterior.
- Para cada serviço da barbearia recuperado do banco de dados, renderize o @components/service-item.tsx (crie o componente). Receba o serviço como prop
